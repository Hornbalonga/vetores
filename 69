#include <stdio.h>

int main(){

int notas[15], i,total=0, media;

printf("Informe as 15 notas: \n");
for(i=0;i<15;i++){

 scanf("%d", &notas[i]);
}
for(i=0;i<15;i++){
 total = total + notas[i];
}
media = (total/15);
printf("A media geral e: %d", media);

return 0;
}
